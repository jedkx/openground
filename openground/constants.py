"""Global constants for OpenGround backend."""

# Default queue sizes
DEFAULT_INGEST_QUEUE_MAXSIZE = 500
DEFAULT_WORKER_QUEUE_MAXSIZE = 1000

# Default timeouts (seconds)
DEFAULT_LOST_TIMEOUT_SECONDS = 5.0

# Default history length
DEFAULT_HISTORY_MAXLEN = 5000

# Add more constants as needed
